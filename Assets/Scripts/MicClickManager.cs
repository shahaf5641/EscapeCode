using UnityEngine;

public class MicClickManager : MonoBehaviour
{
    public static MicClickManager Instance;

    void Awake()
    {
        if (Instance == null)
        {
            Instance = this;
            DontDestroyOnLoad(gameObject);
        }
        else
        {
            Destroy(gameObject);
        }
    }
}
